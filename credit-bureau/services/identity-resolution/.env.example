# HTTP
PORT=4002

# Authentication
IDENTITY_API_KEYS=local-identity-key

# Postgres
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=Advance12
PGDATABASE=credit_bureau
PGSSLMODE=disable

# Kafka consumer
KAFKA_BROKERS=
KAFKA_CLIENT_ID=identity-resolution
KAFKA_GROUP_ID=identity-resolution-group
KAFKA_INGESTION_TOPIC=identity-events
KAFKA_FROM_BEGINNING=false
